import math

def area(radius):
    return math.pi * pow(radius, 2)

def circumfernce(radius):
    return 2 * math.pi * radius

def areaR(length, width):
    return length * width

def hypotenuse(a,b):
    return math.sqrt(pow(a, 2) + pow(b, 2))